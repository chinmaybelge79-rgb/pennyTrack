{
  "name": "expensetrac-root",
  "version": "1.0.0",
  "scripts": {
    "start": "npm start --prefix backend",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "dev": "npm run server & npm run client"
  }
}
